// Copyright (C) 2024 - The Spurte Authors
// 
// The code provided, as well as usage of this code is licensed under the MIT License
//
// For more information, check the LICENSE file at the root of this project, or check https://github.com/nikeokoronkwo/spurte for more information

/// The configuration options for the Spurte JavaScript/TypeScript implementation.
/// 
/// This extends the common options, with implementation-specific options.
module SpurteJSOptions

extends "common.pkl"

import "common.pkl"

// TODO: Add Deno Options

class SpurteJSBuildOptions extends SpurteBuildOptions {
    codesplitting: Boolean?

    // TODO: Add ESBuild
}

/// The path to the deno configuration file, or equivalent JSON file used
denoConfig: String?

/// The Spurte JS Build Options
build: SpurteJSBuildOptions? = new SpurteJSBuildOptions {
    outdir = "dist"
    minify = true
    codesplitting = true
}

/// Options for configuring the usage of npm dependencies
npm: SpurteJSSharedOptions?